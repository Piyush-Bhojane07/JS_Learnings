const readline = require("readline-sync");

const a = parseInt(readline.question("Enter a : "));
const b = parseInt(readline.question("Enter a : "));


function sum (a, b){
    return a + b
}

sum(a, b)